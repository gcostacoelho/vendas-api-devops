import { StateEntity } from "../entities/state.entity";

export const StateMock: StateEntity = {
    createdAt: new Date(),
    id: 1522,
    uf: "SP",
    name: "nameMock",
    updatedAt: new Date(),
}